[project]
name = "docker-workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psycopg2-binary>=2.9.11",
    "sqlalchemy>=2.0.46",
    "tqdm>=4.67.1",
]

[tool.uv.workspace]
members = [
    "pipeline",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
